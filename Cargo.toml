[package]
name = "breaking_net"
version = "0.2.0"
edition = "2024"

[features]
stress_testing=["rand"]
default=["stress_testing"]

[dependencies]
socket2 = "0.6.2"
bitcode = "0.6.9"

rand = {version = "0.9.2", optional=true}